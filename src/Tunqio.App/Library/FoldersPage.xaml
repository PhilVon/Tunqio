<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Tunqio.App.Library.FoldersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Tunqio.App.Library"
    NavigationCacheMode="Enabled">
  <!-- Library › Folders (docs/ui-screens-and-flows.md): the watched folders; a folder opens the Tracks view filtered to it. -->
  <Grid RowDefinitions="Auto,*">
    <TextBlock Text="Folders" Style="{StaticResource SubtitleTextBlockStyle}" Margin="16,12,16,4" />

    <ListView x:Name="List" Grid.Row="1" ItemsSource="{x:Bind ViewModel.Rows, Mode=OneWay}" IsItemClickEnabled="True" SelectionMode="None" ItemClick="OnItemClick">
      <ListView.ItemContainerStyle>
        <Style TargetType="ListViewItem">
          <Setter Property="MinHeight" Value="56" />
          <Setter Property="Padding" Value="16,0" />
          <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        </Style>
      </ListView.ItemContainerStyle>
      <ListView.ItemTemplate>
        <DataTemplate x:DataType="local:FolderRow">
          <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12" AutomationProperties.Name="{x:Bind Name}">
            <FontIcon Glyph="&#xE8B7;" FontSize="20" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
              <TextBlock Text="{x:Bind Name}" TextTrimming="CharacterEllipsis" />
              <TextBlock Text="{x:Bind Detail}" Opacity="0.7" FontSize="12" TextTrimming="CharacterEllipsis" />
            </StackPanel>
          </Grid>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>

    <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="4" MaxWidth="360"
                Visibility="{x:Bind ViewModel.IsEmpty, Mode=OneWay}">
      <TextBlock Text="No library folders yet" Style="{StaticResource SubtitleTextBlockStyle}" HorizontalAlignment="Center" />
      <TextBlock Text="Add one in Settings › Library." Opacity="0.7" TextAlignment="Center" />
    </StackPanel>
  </Grid>
</Page>
